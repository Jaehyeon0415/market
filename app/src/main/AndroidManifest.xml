<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.everymarket">
    <!-- 인터넷 권한 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".PasswordActivity"></activity>
        <activity android:name=".ui.SearchActivity">

            <!-- <intent-filter> -->
            <!-- <action android:name="android.intent.action.SEARCH" /> -->
            <!-- </intent-filter> -->
            <!-- <meta-data android:name="android.app.search" -->
            <!-- android:resource="@xml/activity_search"/> -->
        </activity>
        <activity android:name=".ui.UserFavoriteActivity" />
        <activity android:name=".ui.UserSellDoneActivity" />
        <activity android:name=".ui.UserSellingActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".ui.WriteSellCategoryDetailActivity" />
        <activity android:name=".ui.WriteBuyDetailActivity" />
        <activity android:name=".ui.WriteSellDetailActivity" />
        <activity android:name=".ui.CategoryDetailActivity" />
        <activity android:name=".ui.CardDetailActivity" />
        <activity android:name=".SignUpActivity" />
        <activity android:name=".AuthMailActivity" />
        <activity
            android:name=".SplashActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"></activity>
    </application>

</manifest>